# Plan: Multi-Provider Configuration

**Status:** in-progress
**Created:** 2026-05-03
**Author:** jenpet + planitect

## Goal

Introduce a config file and multi-provider support so voilot can manage different agent backends (OpenCode, Claude Code, Pi in the future). Users can set a global default provider, override it per worktree, and pick a provider when creating a session. Only OpenCode has a concrete implementation for now; the config schema and registry are structured for future providers.

## Context

Phase 1 (provider-registry, `5ac04f3`) introduced `ProviderRegistry` with a single `Provider`. The registry keys instances by `workDir` only, holds one provider, and has no config file — everything comes from CLI flags and env vars. This phase adds the config layer and multi-provider plumbing.

**Dependencies:** Phase 1 provider-registry (complete).

## Config File

**Location:** `~/.config/voilot/config.json`, overridable via `--config` CLI flag.

**Schema:**

```json
{
  "workspace": "/Users/jenpet/workspace",
  "defaultProvider": "opencode",
  "providers": {
    "opencode": {
      "type": "opencode",
      "binary": "opencode"
    }
  },
  "maxInstances": 5,
  "idleTimeout": "10m",
  "ttsUrl": "http://localhost:8880",
  "sttUrl": "http://localhost:5003"
}
```

- `type` maps to a built-in spawn strategy. Supported types: `opencode`. Future: `claude-code`, `pi`.
- `binary` overrides the default binary name for that type.
- `defaultProvider` is the global fallback when no per-worktree default is set.
- `maxInstances` is global across all providers (shared pool).

**Future provider types (no implementation yet):**
- `claude-code` — Claude Code CLI (`claude`).
- `pi` — Pi coding agent.

The server validates that configured provider types have implementations. Unsupported types log a warning at startup but don't prevent the server from starting.

## Config Precedence

```
hardcoded defaults < config file < CLI flags
```

No environment variables. `VOILOT_MAX_INSTANCES` env var is removed.

**CLI flags that remain (deployment-specific):**
- `--port`, `--hostname`, `--data-dir`, `--cors-origins`, `--config`

**CLI flags removed (moved to config, still available as CLI overrides):**
- `--opencode-binary` → `providers.opencode.binary` in config
- `--tts-url` → `ttsUrl` in config
- `--stt-url` → `sttUrl` in config
- `--workspace-dir` → `workspace` in config

**Missing config behavior:** If no config file exists and no `--config` flag is provided, the server prints an error with a sample config and the expected path, then exits. No interactive setup wizard — a sample `config.example.json` is provided in the repo alongside documentation.

## Runtime State Files

All under `<data-dir>/` (CLI flag, default `voilot-data/`):

| File | Key | Purpose |
|------|-----|---------|
| `session-map.json` | sessionID | Worktree path, title, timestamp, **provider name** |
| `worktree-defaults.json` | worktree path | Default provider name per worktree |
| `pids/` | — | Ephemeral PID files, cleaned on startup |

**`worktree-defaults.json` example:**

```json
{
  "/Users/jenpet/workspace/voilot": "opencode",
  "/Users/jenpet/workspace/voilot-feature-branch": "opencode"
}
```

This is runtime state mutated via the UI, not user-edited config.

## Registry Changes

- `ProviderRegistry` holds `map[string]Provider` (keyed by provider name) instead of a single `Provider`.
- Instance key becomes composite: `(workDir, providerName)`.
- `GetOrSpawn(ctx, workDir, providerName)` replaces `GetOrSpawn(ctx, workDir)`.
- `maxInstances` limit is global across all providers.
- LRU eviction considers all instances regardless of provider.

## Symlink Resolution

The workspace directory (`~/voilot-wd`) may contain symlinks to repos in other directories (e.g., `~/dev/jenpet/voilot`). OpenCode resolves symlinks internally when registering its project directory. To prevent path mismatches (e.g., scoping prompt sends symlink path but OpenCode's project uses the real path), the registry resolves symlinks on worktree paths at the boundary — in `GetOrSpawn` and all public methods that accept `worktreePath`. This ensures:

- Instance keys use canonical paths
- PID file workDir uses canonical paths
- Scoping prompts reference canonical paths
- Session map entries use canonical paths

**Implementation:** A `resolveWorktreePath` helper calls `filepath.EvalSymlinks` and falls back to the original path on error. Called at the top of every public `ProviderRegistry` method that accepts `worktreePath`.

## Session Changes

- `Session` struct gains `Provider string` field.
- `sessionmap.Entry` gains `Provider string` field.
- Sessions display a provider badge in the frontend.

## InitializeSession

After creating a session, the handler sends a scoping/welcome prompt via `adapter.InitializeSession(ctx, sessionID, prompt)`. The prompt is generated by `agent.ScopePrompt(worktreePath)` which tells the agent to briefly welcome the user, mention the branch name, and summarize dirty files / recent commits. The adapter is pure transport — it looks up the session's agent/model internally and calls `SendMessageAsync`.

- **`ScopePrompt(worktreePath)`** — package-level function in `adapter.go`, takes only worktree path
- **`InitializeSession(ctx, sessionID, prompt)`** — on the `Adapter` interface, fire-and-forget with logged errors
- **Scoping message detection** — backend hides the scoping message from the UI by pattern-matching "You are working in" + "Briefly welcome the user" in `opencode.go`
- **Worktree default save** — also happens in `handleCreateSession` (last-used pattern), with nil guard for `wtDefaults`

## API Changes

- `handleCreateSession` body gains optional `provider` field. Defaults to worktree default, then global default.
- New endpoints for worktree provider defaults:
  - `GET /api/worktrees/:path/provider` — get worktree default provider.
  - `PUT /api/worktrees/:path/provider` — set worktree default provider.
- Provider list endpoint: `GET /api/providers` — returns configured providers.

## Frontend Changes

- **Session creation form:** provider dropdown, pre-filled with worktree default. Shown when `availableProviders.length >= 1`.
- **Session card:** provider badge pill shown before agent badge when `session.provider` is set.
- **Worktree page:** selected provider passed to `createSession`, worktree default saved on session creation (last-used pattern). No gear icon — default is just "last used".
- **`useProvider.ts` composable:** fetches provider list from `GET /api/providers`, manages worktree default via `GET/PUT /api/worktree-defaults`.
- **`useSession.ts`:** `provider?: string` added to `Session` and `CreateSessionOptions` interfaces.

## Implementation Progress

### Phase 1 — Provider Registry (COMPLETE, committed `5ac04f3`)
- ✅ Full ProviderRegistry with lifecycle management
- ✅ Sub-session filtering, session backfill, project/worktree sorting
- ✅ Frontend updates (StatusIndicator, useSession, worktree page)
- ✅ Tests (14 registry tests, 11 handler tests)

### Phase 2 — Backend (ALL COMPLETE, compiles, all 25 tests pass)
- ✅ `config/config.go` — Config struct, Load(), Validate(), SampleConfig(), DefaultPath()
- ✅ `cmd/server/main.go` — rewritten to use config
- ✅ `agent/registry.go` — multi-provider: map[string]Provider, instanceKey() composite key
- ✅ `agent/worktree_defaults.go` — WorktreeDefaults with Get/Set/All, file-backed JSON
- ✅ `agent/adapter.go` — Provider field on SessionOptions, ScopePrompt(), InitializeSession on Adapter interface
- ✅ `agent/opencode.go` — InitializeSession implemented
- ✅ `agent/agenttest/mock_adapter.go` — InitializeSession mock with call tracking
- ✅ `sessionmap/sessionmap.go` — Provider field on Entry
- ✅ `api/router.go` — wtDefaults field, new routes
- ✅ `api/resolve.go` — rewritten for provider resolution
- ✅ `api/handlers.go` — InitializeSession call in handleCreateSession, worktree default save, provider endpoints

### Phase 2 — Frontend (COMPLETE, builds clean)
- ✅ `composables/useProvider.ts` — new composable
- ✅ `composables/useSession.ts` — provider field added
- ✅ `pages/project/[name]/worktree/[worktree].vue` — provider dropdown, last-used default
- ✅ `components/SessionCard.vue` — provider badge

### Remaining Work
- 🔄 **Symlink resolution** — Add `resolveWorktreePath` helper to registry, call in all public methods that accept `worktreePath`. Add test with temp dir symlink.
- ⬜ **Sample config & docs** — `config.example.json` in repo root, update README.md, AGENTS.md
- ⬜ **Final commit** — All Phase 2 changes on `plan/provider-registry` branch

## Files Changed (Phase 2)

**Backend — new files:**
- `backend/internal/config/config.go`
- `backend/internal/agent/worktree_defaults.go`

**Backend — modified:**
- `backend/cmd/server/main.go` — rewritten for config
- `backend/internal/agent/registry.go` — multi-provider, symlink resolution (pending)
- `backend/internal/agent/adapter.go` — ScopePrompt, InitializeSession interface
- `backend/internal/agent/opencode.go` — InitializeSession impl
- `backend/internal/agent/agenttest/mock_adapter.go` — InitializeSession mock
- `backend/internal/sessionmap/sessionmap.go` — Provider field
- `backend/internal/api/router.go` — wtDefaults, new routes
- `backend/internal/api/resolve.go` — provider resolution rewrite
- `backend/internal/api/handlers.go` — InitializeSession, worktree default save, provider endpoints
- `backend/internal/agent/registry_test.go` — updated for new signatures
- `backend/internal/api/handlers_test.go` — updated for new signatures

**Frontend — new files:**
- `frontend/composables/useProvider.ts`

**Frontend — modified:**
- `frontend/composables/useSession.ts`
- `frontend/pages/project/[name]/worktree/[worktree].vue`
- `frontend/components/SessionCard.vue`

**Config:**
- `~/.config/voilot/config.json` — created for running backend

## Open Questions

None — all design decisions resolved.

## Acceptance Criteria

- [x] Server loads config from `~/.config/voilot/config.json` (or `--config` override).
- [x] Server exits with sample config error when no config found.
- [x] CLI flags removed; values come from config with CLI override support for deployment-specific flags.
- [x] Registry supports multiple providers keyed by `(workDir, providerName)`.
- [x] Per-worktree default provider stored in `worktree-defaults.json`, settable via API.
- [x] Sessions track provider name in session map and API responses.
- [x] Frontend shows provider dropdown on session creation.
- [x] Frontend shows provider badge on session cards.
- [x] InitializeSession sends scoping prompt on session creation.
- [ ] Symlink resolution in registry for consistent canonical paths.
- [ ] `config.example.json` and updated docs in repo.
